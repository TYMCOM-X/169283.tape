MODULE PMPRC(DOLLAR,ENTRIES=($PMPRC),FSAVE,TIMING,TIMER=EXTERNAL(SIX12))=
BEGIN

REQUIRE PDEFS.BLI;

UNDECLARE $PMPRC;

%  THIS IS THE DRIVER ROUTINE TO SET UP AND CONTROL THE PARSER FOR THE
   MIDAS PROCEDURE SYNTAX PROCESSING.
%

GLOBAL ROUTINE $PMPRC=
   BEGIN
   LOCAL TREE;
   $PARSE(P$<0,0>,P$$ROUT<0,0>,TREE<0,0>);
   END;

END ELUDOM
