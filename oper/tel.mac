; FUNCTION/SUBROUTINE  TO RETURN TRUE IF TTY INPUT WAITING,ELSE FALSE
;    K=TEL(N)   WILL SET K=-1 (AND N)   IF TELETYPE INPUT WAITING, ELSE 0
;   0 IS  SAME SAME  FALSE, -1 SAME SAME TRUE   ON THE 10

        ENTRY TEL
TEL: 0
        SETO          ;ASSUME TRUE
        TTCALL 13,0
        SETZ       ;UNTIL PROVEN OTHERWISE
        MOVEM (16)
        JRA 16,1(16)
        END
