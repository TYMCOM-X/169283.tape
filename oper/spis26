        LOGICAL FUNCTION NMFIND(NOARGS)
      COMMON/INDATA/NUMEL,NUNODS,NUMNOD,NOSTOP,JELCNT(20),LOCATE(21),
     1   ICURNT(21),JUNODE(101),NAME(100),LOCAL(100),MNAME(100)
C
        DOUBLE PRECISION MODNAM,MNAME,NAME
C
C
        COMMON/PRISET/ITEM,NAMELE,ISTART,ISTOP,IPALL,ITHPAR
        DOUBLE PRECISION NAMELE,DTEMP
        LOGICAL IPALL,EQUAL
C
        NMFIND=.FALSE.
        IF(ISTART.GT.ISTOP)RETURN
        DO 10 I=ISTART,ISTOP
        IF(EQUAL(NAME(I),NAMELE))GO TO 20
10      CONTINUE
        RETURN
20      NMFIND=.TRUE.
        ISTART=I
        ISTOP=I
        RETURN
        END
